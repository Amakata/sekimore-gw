# Sekimore Gateway - Environment Variables
# Optional: Only needed if using upstream proxy with authentication

# Upstream Proxy Authentication (optional)
# Uncomment and set these if connecting through a corporate proxy
# SEKIMORE_UPSTREAM_PROXY_USERNAME=your-username
# SEKIMORE_UPSTREAM_PROXY_PASSWORD=your-password
