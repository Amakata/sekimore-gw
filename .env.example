# Sekimore Gateway - Environment Variables Example
# Copy this file to .env and customize for your environment

# Docker Compose Project Name
# This is used for multi-organization isolation
# Each organization/project should have a unique name
COMPOSE_PROJECT_NAME=sekimore-gw

# Network Names (optional, defaults provided)
# Change these if you need custom network names
# INTERNAL_NETWORK_NAME=internal-net
# INTERNET_NETWORK_NAME=internet

# Upstream Proxy Authentication (optional)
# Use these if connecting through a corporate proxy
# SEKIMORE_UPSTREAM_PROXY_USERNAME=your-username
# SEKIMORE_UPSTREAM_PROXY_PASSWORD=your-password

# Example for multi-organization setup:
# Organization A:
#   COMPOSE_PROJECT_NAME=sekimore-org-a
# Organization B:
#   COMPOSE_PROJECT_NAME=sekimore-org-b
