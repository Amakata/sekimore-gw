#!/bin/sh
# sekimore — the former name of sgw-agent, the AI's command for the relay (#257). It stays for a
# few releases so that guides and habits have time to move; everything it did (the env file,
# the token refresh) is in the binary now.
exec /usr/local/bin/sgw-agent "$@"
